class Solution(object):
    def findMedianSortedArrays(self, nums1, nums2):
        sum = sorted(nums1 + nums2)
        if len(sum) % 2 == 0:
            return (sum[len(sum) // 2 - 1] + sum[len(sum) // 2]) / 2.0
        else:
            return sum[len(sum) // 2]
