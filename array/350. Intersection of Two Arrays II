class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        from collections import Counter
        counts1 = Counter(nums1)
        counts2 = Counter(nums2)
        intersection = list((counts1 & counts2).elements())
        return intersection
